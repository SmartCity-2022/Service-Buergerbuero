describe("lost_property", () => {
    describe("get", () => {
        describe("given no type parameter is set", () => {
            it("should return the whole table", () => {
                expect(true).toBe(true);
            });
        });
    });
});
